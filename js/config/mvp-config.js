// MVP Configuration for Let It Out Features
const MVP_CONFIG = {
    // MVP Mode: All LET IT OUT premium features free
    MVP_MODE: true,
    
    // Hide prompts and Unlock More Prompts for MVP (restore later)
    HIDE_PROMPTS_FOR_MVP: true,
    
    // Premium Packs: Manual unlock mode for engagement
    FREE_PREMIUM_PACKS: false,
    MANUAL_UNLOCK_MODE: true,
    
    // Post Reply Unlocks: No paywall, unlimited access
    FREE_POST_UNLOCKS: true,
    
    // Unlimited unlocks: No 3-free limit for post replies
    UNLIMITED_UNLOCKS: true
};

// Export for global use
window.MVP_CONFIG = MVP_CONFIG; 